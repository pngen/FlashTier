# Normalize line endings: text files use LF in the repository.
* text=auto

# Batch/PowerShell helpers keep platform line endings where relevant.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf
*.sh text eol=lf
